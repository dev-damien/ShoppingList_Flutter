import 'package:flutter/material.dart';

class ListDetailBody extends StatelessWidget {
  const ListDetailBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
